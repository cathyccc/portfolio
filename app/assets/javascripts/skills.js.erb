$(document).on('turbolinks:load',function(){
  // if running on firefox;
  if (typeof InstallTrigger !== 'undefined'){
    var imacSource = "<%= image_path ('computer-imac.svg') %>";
    var imacIcon = $('<img>').attr('src', imacSource);
    $('#imac-icon').html(imacIcon);

    var cupSource = "<%= image_path ('designer-cup.svg') %>";
    var cupIcon = $('<img>').attr('src', cupSource);
    $('#cup-icon').html(cupIcon);
  }
  // if (document.location.pathname == "/skills") {
    // var imac = new Vivus('imac-icon',{
    //   type: 'oneByOne',
    //   duration: 200,
    //   file: "<%= image_path ('computer-imac.svg') %>"
    // }, function(obj){
    //   obj.el.classList.add('finished');
    // });
    //
    // setTimeout(function(){
    //   new Vivus('cup-icon',{
    //     type: 'oneByOne',
    //     duration: 200,
    //     file: "<%= image_path ('designer-cup.svg') %>"
    //   });
    // }, 1800);
  // }
});
